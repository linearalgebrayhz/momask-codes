# CLaTr requirements compatible with PyTorch 1.12.0 + CUDA 11.3
# CRITICAL: Use pytorch-lightning 1.7.x (NOT 2.x) for PyTorch 1.12.0 compatibility

# PyTorch Lightning - pinned to 1.7.7 (last 1.x version, compatible with torch 1.12.0)
pytorch-lightning==1.7.7

# Hydra - pinned to 1.2.0 (stable version from 2022, compatible with old torch)
hydra-core==1.2.0

# Already installed in TKCAM environment (safe to reinstall)
einops>=0.6.1
scikit-learn
wandb

# New packages (no torch dependency, safe)
evo
rich
torchtyping